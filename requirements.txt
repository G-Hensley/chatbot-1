# Core dependencies for the chatbot
requests>=2.28.0

# FastAPI Web Server Dependencies (lighter versions for Railway)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Optional heavy dependencies (not needed for Railway deployment)
# datasets>=2.0.0
# transformers>=4.20.0
# torch>=1.12.0
# python-dotenv>=1.0.0
