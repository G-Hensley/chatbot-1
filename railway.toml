# Railway Deployment Configuration
# Let Railway auto-detect the Python project

[deploy]
startCommand = "uvicorn web_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
